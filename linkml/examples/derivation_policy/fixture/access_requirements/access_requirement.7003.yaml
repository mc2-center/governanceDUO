# Fixture only: a Controlled AccessRequirement shared by several inputs, so their
# combination is not disjoint and any review comes from the DerivationRule.
id: access_requirement.7003
dataTier:
  - Controlled
